<UserControl x:Class="EMx.UI.Controls.SelectFilepathControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:EMx.UI.Controls">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*"/>
      <ColumnDefinition Width="30"/>
    </Grid.ColumnDefinitions>
    <TextBox x:Name="txtValue" Margin="1" Style="{StaticResource {x:Static ToolBar.TextBoxStyleKey}}"
             BorderBrush="LightGray" BorderThickness="1" HorizontalContentAlignment="Left"
             VerticalContentAlignment="Center" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
    <Button Click="btnSelectFile_Clicked" Grid.Column="1" Margin="1" HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch" Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"
            BorderBrush="LightGray" BorderThickness="1">...</Button>
  </Grid>
</UserControl>